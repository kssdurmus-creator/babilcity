import 'package:flutter/material.dart';

class VipHistoryPage extends StatelessWidget {
  @override Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('VIP History')), body: Padding(padding: EdgeInsets.all(16), child: Column(children: [Text('Bulut arşivi (VIP) - placeholder')])),);
  }
}